import { DaytimeIconPipe } from './daytime-icon.pipe';

describe('DaytimeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new DaytimeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
